spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bot_father
    username: admin
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update